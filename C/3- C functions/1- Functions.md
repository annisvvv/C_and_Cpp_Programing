# Create a function
Outside the main function we declare our function name followed by parenthesis `()` and we write the code to execute when we call the function inside `{}`.

```
void _myFunction_() {  
  // code to be executed  
}
```

