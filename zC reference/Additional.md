# Default syntax
```
#include <stdio.h>  
  
int main() {  
	#code
	
	return 0; 
}
```
# Escape sequence
`\n` : New line
`\t` : Create a horizontal tab
`\\` : Inserts a backslash character (\)
`\"` : Insert a double cote character

# Comments
`//` : Write a comment
`/*` # your code `*/`